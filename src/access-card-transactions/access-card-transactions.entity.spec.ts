import { AccessCardTransactions } from './access-card-transactions.entity';

describe('AccessCardTransactionsEntity', () => {
  it('should be defined', () => {
    expect(new AccessCardTransactions()).toBeDefined();
  });
});
